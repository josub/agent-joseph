# Episodic — 2026-06-24

Raw dated log / audit. Append-only.

## sleep — nightly consolidation (cycle covering 2026-06-23 activity)

### Consolidated working memory (2 notes)
- **`2026-06-23-design-system-prompt-saas.md`** — a user-provided SaaS design-system
  prompt (brief-first method, the three AI design defaults, principles, copy rules, code
  floor, session format). Integrated into a new convention page
  `semantic/design-method-distinctive-ui.md` (`type: convention`). Note anticipated this
  promotion. Archived to `memory/archive/2026-06-24/`.
- **`2026-06-23-ui-prototype-build-lessons.md`** — reusable engineering lessons from the
  lead-scanner build (single-file React+Tailwind recipe, the Babel automatic-vs-classic
  JSX-runtime fix, the puppeteer-core screenshot-verify loop, the WCAG-AA contrast fix).
  Integrated into a new topic page `semantic/web-ui-prototype-recipe.md` (`type: topic`).
  Archived to `memory/archive/2026-06-24/`.

### Swept completed work (1 item)
- **`lead-scanner-ui-prototype`** (filed + worked 2026-06-23; "Tessera" badge-scanner
  prototype — 5-screen Shazam-style flow, single self-contained React+Tailwind
  `index.html`, verified end-to-end in headless Chrome, zero page errors). Its reusable
  knowledge was already captured in the build's working note and is now consolidated into
  `semantic/web-ui-prototype-recipe.md` (cited to this build) — so no duplicate page was
  created. Folder moved `work/completed/` → `work/archived/2026-06-24/`.

### Wiki maintenance
- `semantic/index.md` updated: +1 Topic (Web UI Prototype Recipe), +1 Convention (Design
  Method — distinctive, brief-first UI).
- Repointed the inert draft skill `skills/design/draft_prototype-ui-artifact/` cross-link
  from the now-archived working note to the two new semantic pages (link fix only; skill
  stays `draft_`/inert per guardrail 6).

### Pattern detection (gated growth)
- No new `draft_` skill or inactive routine minted this cycle. The design theme's
  procedural ability already exists as `skills/design/draft_prototype-ui-artifact/`
  (created 2026-06-23 on user request) and remains pending keep/edit/discard at
  `morning-review`; one prototype task is not yet a repeated pattern warranting more.
- Still pending from the prior cycle: `skills/knowledge/draft_capture-resource` (awaiting
  morning-review).

### Reports
- Rolled the previous cycle's report `memory/reports/latest/sleep-report.md`
  (Sleep Report — 2026-06-22) → `memory/reports/2026-06-22/`.
- Wrote a fresh `memory/reports/latest/sleep-report.md` for this cycle.

### Housekeeping
- Pruned scratch older than 30 days: none due (oldest is `memory/archive/2026-06-22/`).
- Ran `scripts/sync-claude-skills.sh` to refresh `.claude/skills/` native exposure (14 linked, 0 pruned).

---
**sleep summary:** consolidated 2 working notes + 1 completed item into 2 new semantic pages (1 convention, 1 topic); rolled the 2026-06-22 report and wrote a fresh sleep report; no new drafts minted; 2 drafts await morning-review.

## morning-review — 2026-06-24

### Draft decisions (gated growth)
- **`draft_capture-resource` → KEEP/promote.** User chose keep. `git mv`
  `skills/knowledge/draft_capture-resource` → `skills/knowledge/capture-resource`;
  stripped `draft_`, updated Notes (DRAFT/inert → promoted). No name collision.
- **`draft_prototype-ui-artifact` → KEEP/promote.** User chose keep. `git mv`
  `skills/design/draft_prototype-ui-artifact` → `skills/design/prototype-ui-artifact`;
  stripped `draft_`, updated Notes. **Establishes the new `design` theme.** No collision.
- Neither skill wired to a routine/workflow (promotion ≠ scheduling).
- Ran `scripts/sync-claude-skills.sh` to expose both promoted skills immediately
  (16 linked, 0 pruned) — both now discoverable to Claude Code.

### Procedure change (user-directed)
- User flagged that promoted skills should go live at promotion time, not wait for nightly
  `sleep`. Updated `skills/innate/morning-review/SKILL.md` (step 2) to run
  `scripts/sync-claude-skills.sh` whenever a draft is promoted, and updated `CLAUDE.md`
  (brain-map row + the `.claude/skills/` convention) to document on-promotion sync. Sync
  is the sanctioned mechanism; links still never hand-authored.

### Focus / priorities
- No mission in focus — `work/missions/` holds only the template. Flagged to user as the
  day's highest-leverage action (define a first mission).
- Work queue empty — `work/tasks/` and `work/alerts/` hold only templates; nothing
  ongoing. `start-work-session` has nothing to claim.

### Reports
- Wrote `memory/reports/latest/morning-report.md`.

---
**morning-review summary:** promoted both pending drafts (capture-resource → knowledge,
prototype-ui-artifact → new design theme) via `git mv`; no missions or queued work yet —
flagged "define a first mission" as today's top need; wrote the morning report.

## Decision: adopt a wiki-level `log.md` (resolves the long-standing flag)

- **Context.** The "No `log.md`" divergence had been flagged in every report since
  2026-06-22. While discussing it, clarified a factual error in the earlier framing:
  `log.md` is a **core** convention in Karpathy's LLM wiki but only an **optional** file
  in OKF — so it's more a Karpathy signature than an OKF one, and our no-`log.md` setup
  was already OKF-compatible. Karpathy is the *furthest* from the old stance, not closest.
- **User decision.** Go with Karpathy fidelity: add a single **wiki-level** changelog,
  not per-folder logs, and not by replacing `episodic/`/`reports/` (which serve different
  jobs).
- **Done:**
  - Created `memory/semantic/log.md` — append-only, machine-parseable
    (`## [date] action | Title`), **backfilled** across both cycles (7 pages + index
    events + the supersede + its own creation).
  - Superseded `memory/semantic/no-logmd-stance.md` from "we omit log.md" → "wiki-level
    log.md adopted" (filename kept for link stability; reversal recorded in `## History`).
  - Fixed now-false cross-page annotations (`index.md`, `llm-wiki-karpathy.md`,
    `open-knowledge-format.md`, `filesystem-as-agentic-architecture.md`); added a
    changelog pointer to `index.md`.
  - Wired `skills/innate/sleep/SKILL.md` (steps 1–2) to append a `log.md` line per page
    touched, and `skills/innate/deep-sleep/SKILL.md` (step 2) to append a `lint` line.
  - Updated `CLAUDE.md` Conventions: documented the wiki-level `log.md` (no per-folder
    logs).
  - Marked the flag **RESOLVED** in `memory/reports/latest/sleep-report.md`.

---
**decision summary:** resolved the `log.md` flag — adopted a single wiki-level
`memory/semantic/log.md` (Karpathy-style), backfilled it, superseded the old stance,
repaired cross-links, and wired `sleep`/`deep-sleep` to maintain it going forward.
